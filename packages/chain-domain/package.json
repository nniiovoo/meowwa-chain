{
  "name": "@meowwa/chain-domain",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Chain-side vocabulary, schemas and pure rules shared by the wallet, mandate and settlement code.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "zod": "4.4.3"
  }
}
